# API токен для доступа к Octo Browser API
OCTO_API_TOKEN="ВАШ_API_ТОКЕН_ЗДЕСЬ"

# (Опционально) Имя файла с прокси (по умолчанию: proxies.csv)
PROXY_FILE="proxies.csv"

# (Опционально) Имя файла с cookies (по умолчанию: cookies.json)
COOKIE_FILE="cookies.json"

# (Опционально) Количество профилей для создания (0 = по количеству прокси)
PROFILE_COUNT="0"
